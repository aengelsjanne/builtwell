{
  "name": "impecable-afterhours-src",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AFTERHOURS — client-facing marketing landing page (static site)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "standalone": "node build-standalone.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
